// The Artists index (server component) — /art/artists. The interactive body
// lives in the client component.

import { ArtArtistsPage } from './art-artists-page'

export default function ArtArtists() {
  return <ArtArtistsPage />
}
